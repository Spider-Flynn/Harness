# Harness Engineering TODO

> 当前阶段：项目结构与整体设计重构。Harness 尚未启用，也未经过真实业务项目验证。

本文只维护当前有效的实施顺序、任务状态、依赖、产出和验收门禁。稳定项目规则归
`AGENTS.md`，项目说明归 `README.md`，系统设计归 `docs/`，业务项目运行合同归
`runtime/HARNESS.md`，具体能力规则归对应 Skill。

## 总体目标

把当前七个子系统的初步草案整理为职责清楚、按意图与风险组合、可接入业务代码仓库、可验证并
可受控演进的 Harness Engineering 系统。

当前七个职责域、横向能力和现有 Skills 是整体重构的输入，不在系统设计确认前把系统数量、编号
目录或当前实现反向固化为最终架构。

## 目标项目结构

```text
Harness/
├── AGENTS.md              # Harness 仓库自身的维护规则
├── README.md              # 项目定位、当前状态和使用入口
├── TODO.md                # 当前实施计划、门禁和未决问题
├── docs/
│   └── 系统设计.md        # 当前有效的稳定系统设计
├── runtime/
│   └── HARNESS.md         # 唯一业务项目运行合同
├── skills/                # Harness 能力源码
└── scripts/               # 仓库维护和业务项目接入脚本
```

`runtime/HARNESS.md` 保持加载方式无关。未来可以由接入脚本生成到业务项目 `AGENTS.md` 的受管
片段，也可以通过运行环境在对话前加载；在平台能力和真实项目接入验证前不提前确定具体方式。

## P0 项目结构与事实载体收敛

状态：已完成（待提交）

目标：删除过程性、重复或已经退出当前架构的载体，让每类事实只有一个明确归属。

待办：

- [x] 确认新的最小目录结构和各入口文件职责。
- [x] 将有效的实施事项与未决问题收敛到本文，删除过程性实施文档。
- [x] 将仍有效的 Skill/系统职责原则迁入 `docs/系统设计.md`，不迁移旧目标拓扑。
- [x] 将系统级进入、跳过、反馈和路由边界收敛到 `runtime/HARNESS.md` 与 `router` Skill，删除独立流程目录。
- [x] 将失效、回流、恢复、终止、授权、审计和最小证据规则收敛到 `runtime/HARNESS.md`，删除重复 runtime 文件。
- [x] 将原运行记录草案中仍需要的最小产物与运行证据字段带入 HARNESS 合同。
- [x] 将项目接入说明并入 README，使 `scripts/` 只保留可执行维护工具。
- [x] 保留既有专用 Agent 文档删除，并清理失效引用；不把 Prompt 角色写成平台权限。
- [x] 删除与当前架构冲突的旧图，待整体编排确认后再生成新图。

验收门禁：

- 根目录入口、稳定设计、运行合同、Skill 和脚本各自只有一个清楚职责。
- 全仓不再引用已删除的流程目录、实施文档、重复 runtime 文件或根 `agents/` 文档（TODO 的迁移说明除外）。
- 没有把旧目标设计、当前草案实现或未验证能力写成最终事实。

## P1 整体架构与设计原则重构

状态：进行中；框架与软件工程实例边界、控制面、职责归属和总体原则已确认，职责域最终拓扑仍待收敛。

目标：先定义 Harness 整体是什么、解决什么问题、由哪些稳定职责构成，再决定具体能力拓扑。

待办：

- [x] 确认项目定位、使用者、适用任务和不做范围。
- [x] 确认业务项目交付面与 Harness 自维护控制面的边界。
- [ ] 重新审视七个职责域、横向能力和额外能力；保留有效职责，不把数量和编号当成目标。
- [x] 定义 Skill、HARNESS、项目规则、脚本、Subagent 和人工决定各自承担什么。
- [x] 定义按意图、上下文和风险进入或跳过能力的原则，不设置固定七段主链。
- [x] 定义确定性规则优先、真实证据优先、最小上下文、可追溯和可回退原则。

验收门禁：

- 系统只有一套架构语义，不再同时存在按需组合与固定全链。
- 每项职责有唯一所有者，能力规则与跨系统编排不混写。
- 稳定系统设计足以约束后续 runtime 和 Skill 二次实现。

## P2 HARNESS 运行合同与必要信息交接

状态：进行中；已根据讨论收敛首版运行循环，仍待真实任务校准。

目标：把 `runtime/HARNESS.md` 设计成唯一、自包含、可投影的业务项目运行合同。

待办：

- [ ] 定义能力进入、跳过、停止和当前负责方的运行判断，不预设独立状态对象。
- [ ] 定义跨能力交接的最小内容：当前目标、范围、约束、相关事实、产物引用、证据和缺口。
- [ ] 根据真实跨能力、跨会话和审计需要定义必要引用与证据，不预设通用对象或状态 Schema。
- [ ] 用真实任务校准反馈、重新规划、人工等待、终止和重新审计边界。
- [ ] 定义独立审计、人工决定、外部写入授权和真实回执边界。
- [ ] 定义最小运行证据；暂不提前选择 Markdown、JSON、JSONL 或其他持久化实现。
- [ ] 保持 HARNESS 源文本唯一；任何业务项目受管片段都由它生成，不维护第二份手写合同。

验收门禁：

- HARNESS 不依赖外部流程文件或其他 runtime 文件即可说明完整公共运行语义。
- HARNESS 不包含 Skill 内部方法、仓库 TODO、接入脚本实现或真实任务记录。
- 短任务和复杂任务都能根据同一合同判断进入、跳过、反馈和停止。

## P3 Skill 与整体编排二次实现

状态：进行中；`router`、`coding` 和 `audit` 已按确认边界收敛，其余能力仍待逐项讨论与校准。

目标：基于统一架构和运行合同重新实现能力，不再从当前目录名称反推目标设计。

待办：

- [ ] 建立目标能力清单，明确固定、条件、本机和控制面能力。
- [ ] 逐项确认 Skill 的触发、排他边界、输入、输出、权限、副作用和结束状态。
- [x] 收敛 `router` 合同：选择具体能力、顺序、稳定产物、测试策略和审计门禁，但不调用能力。
- [ ] 收敛执行系统目标拓扑，解决旧 `design/dev/fix` 与当前六个执行 Skill 的冲突。
- [ ] 保证功能型 Skill 只返回自身结果、问题、仍有效事实和恢复条件，不自行跨系统跳转。
- [ ] 让 HARNESS 根据当前需求、Router 路线、能力返回和反馈完成调度、回收与重新规划。
- [ ] 继续隔离 Harness 演进控制面，不让 `retro` 或维护能力进入业务需求链。

验收门禁：

- 系统设计、HARNESS、Skill 元数据、Skill 正文和实际目录使用同一能力拓扑。
- 所有跨系统调度、反馈与重新规划只由 HARNESS 解释，所有能力内部方法只由对应 Skill 维护。
- 隔离 Case 能证明节点可跳过、反馈返回正确所有者、变化只使依赖产物和审计失效。

## P4 中间产物、运行证据与持久化实现

状态：待开始；依赖 P2，可与 P3 后半段迭代。

目标：根据真实交接需要实现中间产物和运行证据管理，不把所有任务强制落成固定文档链。

待办：

- [ ] 区分只在会话内有效、跨会话必须保存和长期进入项目知识的产物。
- [ ] 真实任务证明需要时，再确定产物引用、版本、依赖和失效传播的实现方式。
- [ ] 让审计、反馈、人工修改和交付回执绑定具体对象版本。
- [ ] 确定运行记录的载体、位置、脱敏、保留和清理规则。
- [ ] 用真实任务数据决定是否需要追加事件流、评测集或自动化回放。

验收门禁：

- 不以文件存在代替产物有效，不以模型自述代替验证或外部结果。
- 修改、反馈、审计和恢复可以追溯到具体对象、版本与证据。
- 普通短任务不会因持久化规则产生不必要的项目污染。

## P5 接入与项目维护工具二次实现

状态：待开始；依赖 P2、P3。

目标：让 `scripts/harness.py` 只固化已经确认的仓库维护与业务项目接入合同。

待办：

- [ ] 让接入工具只管理 `runtime/HARNESS.md`、受批准的 Skill 投影和受管清单。
- [ ] 从唯一能力清单发现 Skill，移除旧 `design/dev/fix` 和多 runtime/flows 文件硬编码。
- [ ] 区分可发现、默认接入、条件接入、本机禁止投影和控制面能力。
- [ ] 永不把本机 `cooper`、`mock` 目录纳入公开仓库；其业务项目投影必须另有明确条件和授权。
- [ ] 设计 HARNESS 到业务项目的加载适配层，不在源合同中绑定 AGENTS 嵌入或 Hook 方案。
- [ ] 验证 `init/doctor/update/remove` 的首次、重复、冲突、升级、回滚和解除行为。

验收门禁：

- 临时 Git 仓库中的接入、检查、更新和移除均通过，且不覆盖业务项目普通文件。
- Manifest、HARNESS 和 Skill 投影与当前仓库版本一致。
- 工具只证明物理接入和确定性检查，不声称真实 Harness 行为已经通过。

## P6 整体验收、真实试点与启用判断

状态：待开始；依赖 P3、P4、P5。

目标：先完成静态与隔离验收，再通过低风险真实任务决定哪些能力可以启用。

待办：

- [ ] 检查目录、引用、Skill 元数据、HARNESS 和接入清单一致性。
- [ ] 验证干净上下文、只读边界、授权门禁和外部回执的真实平台能力。
- [ ] 用一个低风险正常任务验证最短有效路径。
- [ ] 用一个审计失败、人工反馈或意图变化任务验证问题回流。
- [ ] 记录正确性、人工打断、返工、耗时、上下文成本、未覆盖范围和维护成本。
- [ ] 只把已有真实行为证据支持的能力标记为启用。

验收门禁：

- 正常路径与至少一条问题回流路径均由同一 HARNESS 入口完整结束。
- 真实验证、独立审计、人工决定和外部操作结果可追溯。
- 未验证能力继续标记为草案或未启用，不用静态检查替代真实任务结论。

## 当前未决问题

- 七个职责域与现有编号目录是否继续作为最终架构，需在 P1 基于职责重新判断；P1 前不移动
  `skills/`。
- `runtime/HARNESS.md` 最终通过业务项目 `AGENTS.md` 受管片段还是运行环境对话前加载，留到
  P5 结合平台能力和真实接入验证决定。
- 审计只读和外部操作拦截能否形成平台级硬约束，留到 P6 实测；当前不假设存在特定 Hook 接口。
