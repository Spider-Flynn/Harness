# Harness Engineering TODO

> 当前阶段：先完成软件工程 Harness 的设计闭环与二次实现，再进入真实项目验证。
> 本文按用户实际使用链路组织当前系统实现，全程操作约束单列；不安排可选的 GitHub 仓库自动化。

## 已确认边界

- Harness 只约束 Agent 在计算机世界中的工作，不建模、限制或替用户决定人工验收、风险接受、发布
  和回退。用户的新输入是否继续原任务，由对话自然表达，不建设持久化“任务结束状态”。
- 宿主主会话是默认执行主体；Skill 是主会话加载的能力与规则单元，不是角色或员工。代码实现属于
  Agent 自身能力，Harness 只需要提供正确链路、规则和约束，不建设教授 Java、API 等基础编码知识的
  Skill。Subagent 只在需要上下文隔离、并行或独立判断时创建。
- `build` 是可选的项目知识建设能力，不是 Harness 启用门禁。没有知识库的项目仍可完成需求；任务前
  跳过知识检索，任务完成后仍可按需沉淀本次形成的长期知识。
- 当前不建设通用 Task、Action、状态机、任务结束状态、知识版本或敏感信息治理系统。进入 Harness 的
  信息源视为已经完成必要处理并允许 AI 使用；业务项目和宿主平台原有权限、安全与凭据规则仍然有效。
- 当前只设计并实现已有软件工程主链。CLI、MQ、前端、数据库迁移等更多专项验证能力不作为当前闭环
  前置条件，后续由真实使用和演进证据决定是否新增。
- 真实任务验证属于设计与二次实现完成后的阶段；当前不以“尚未实测”否定逻辑设计，也不把静态设计
  写成已经可用。
- GitHub CI、CD 和 Release 自动化只是可选的 Harness 仓库维护增强，与系统运行无关，不进入当前
  实施顺序；未来确有维护需求时再单独提出。

## 全程适用的操作约束

- [x] Agent 执行 Commit、Push、PR、提测、部署、发布或其他外部写入前，必须获得覆盖当前动作、目标和
  范围的明确用户授权；人工自行执行的操作不由 Harness 跟踪或限制。
- [x] 明确确定性保护边界：Git Hooks 只保护分支策略，不能证明用户授权；外部操作优先使用宿主审批。
  命令包装器只有在宿主能阻止绕过、使其成为唯一入口时才构成强制门禁。
- [x] Agent 只报告实际执行并观察到的结果；失败或部分成功时停止后续依赖动作，说明已发生的外部
  状态、失败点和未完成项，不自动重试、回退或扩大授权。
- [ ] 在选定具体宿主后实现并验证外部操作审批适配；当前只有授权规则与分支 Hook，尚无已验证的
  通用授权门禁。

## 1. 用户入口、系统选择与项目绑定

目标：用户能够选择 Harness 领域系统和代码项目，并由确定性工具完成可重复的绑定。

### 1.1 产品入口

- [x] 第一版采用“确定性 CLI 核心 + AI 辅助交互”。暂不建设 TUI；用户可以直接运行 CLI，也可以由
  AI 完成灵活判断和参数组织后调用同一工具。
- [x] 所有绑定结果统一由 `scripts/harness.py` 产生，不维护 CLI、仓库脚本和 AI 三套实现。
- [x] 保留薄 CLI 入口，按命令拆分 `scripts/harness_cli/commands/`，系统与能力发现、绑定清单、规则
  片段、存储和文件操作分别维护；模块职责与 `cmd/` 对应关系见 `docs/接入设计.md`。已由独立
  复核对比拆分前后的五个命令、临时项目完整接入/解绑、初始化回滚与内容冲突场景，行为保持一致。
- [x] README 聚焦用户理解和使用；AGENTS 维护长期项目规则，进度归 TODO，接入内部设计归专门文档。
- [x] 接入提示词收敛到 `cmd/`，AGENTS 按需加载 `init/relink/doctor/remove/list` 对应文件；
  只保留交互和调用指引，确定性规则归 Python。当前仅完成文档与脚本合同核对。
- [ ] 验证真实接入对话：仅输入 `init`、给出明确项目、缺少规则入口、多项目选择、已有绑定、检查失败
  与解绑反馈；确认 AI 不重复提问、不误触发或越权修复，再确认对话入口可正式启用。
- [x] 建立 `systems/software-engineering.json` 领域实例清单。当前只有软件工程时自动选择；未来新增领域
  系统只增加 Manifest 和对应资产，不把当前五个职责域固化为框架结构。
- [x] 脚本默认要求目标目录存在 `.git`，并确定性检查目录、受管路径和内容冲突；AI 或用户确认非 Git
  目录仍适用时，必须显式传入 `--allow-non-git`。

### 1.2 Runtime 入口绑定

- [x] 第一版使用项目规则引导：向已有 `AGENTS.md`、`CLAUDE.md` 写入受管入口；两者都存在时分别维护，
  两者都不存在时交互说明生态并询问创建哪一个，非交互模式必须用 `--rules` 明确指定。
- [x] 规则文件只保存最小加载片段；`.harness/runtime/HARNESS.md` 软链接到本仓库
  `runtime/HARNESS.md`，保持 Runtime 唯一来源，不复制独立版本。
- [x] 移除复制 Runtime 的旧实现。规则入口是当前所有宿主都能使用的保底加载器。
- [ ] 会话前 Runtime Hook 加载器和 TUI 暂不实现。只有真实使用证明规则入口会失效、且目标宿主具有稳定 Hook 能力时，
  再比较上下文污染、重复 Token、时延、可观察性和失败降级后增加宿主适配器。

### 1.3 Skill 绑定

- [x] 将当前领域系统选择的 Skills 直接软连接到目标项目 `.agents/skills/<skill>`，目标项目内不再按
  子系统增加目录层级。
- [x] 由领域 Manifest 维护六类能力清单：`default`、`conditional`、`local`、`horizontal`、`personal`、
  `evolution`。当前系统自动投影默认与横向能力，条件与本机能力按需选择，个人与演进能力不进入业务项目。
- [x] `cr` 和演进能力不进入业务项目；`biz`、`cooper`、`mock` 通过可重复的
  `--with-skill <name>` 选择，不可用或不属于当前领域的能力会被确定性拒绝。

### 1.4 `init`、`relink` 与绑定记录

- [x] `init` 在目标项目 `.harness/manifest.json` 记录领域系统、目标项目、Runtime、Skills、规则入口、
  可选能力和受管 Git Hook；本仓库 `data/projects.json` 只提供 `list` 索引，目标项目清单才是绑定事实。
- [x] 本地辅助数据统一放在仓库 `data/`：项目索引、`locks/` 和按需生成的 `backups/`，整体 Git 忽略，
  不写入用户主目录；路径以 Harness 源目录为基准，不受命令执行目录影响。
- [x] `relink` 复用原项目清单，重新建立 Runtime、Skills、规则片段和本机索引，不再次要求选择。
- [x] 绑定其他项目或其他领域系统仍使用新的 `init`，`relink` 不接受改变绑定目标的选择参数。
- [x] Harness 源目录移动或软链接损坏后，从新源码位置运行 `relink --project <原项目>` 重建绝对链接；
  `doctor` 会给出明确修复提示。
- [x] 命令收敛为 `init/relink/doctor/remove/list`，不再保留与实时软链接语义重复的 `update`。
- [x] 项目操作和本机索引使用文件锁，写入使用原子替换；`init/relink/remove` 在可捕获失败时恢复原内容，
  普通文件冲突一律拒绝覆盖。
- [x] 知识规范封装为 `rules-know`，随默认 Skills 投影到 `.agents/skills/rules-know/`，不再单独投影
  知识资源。旧绑定由 `relink` 补齐规则 Skill 并清理原受管资源链接；解绑保留 `.harness/know/`
  和 `tasks/`，已有知识不阻止再次初始化。
- [ ] 补充进程被强制终止后遗留半完成绑定的识别与恢复；这不影响正常返回或异常路径的当前回滚。

完成标准：用户能够从一个入口完成“选系统—选项目—确认入口文件—绑定 Runtime—绑定 Skills”，并可
在不重新选择的情况下修复原绑定；未来新增领域系统不需要重写整套接入工具。

## 2. 可选的项目知识建设 `build`

目标：用户需要时，通过连续问答建设项目知识；不执行 `build` 也不阻断正常需求。

### 2.1 入口和两类项目

- [x] 删除接入脚本中“首次启用前必须执行 `$build`”的强制提示，改为可选引导。
- [x] 新项目与存量项目均先索要或确认用户信息源，通过问答补齐；其他来源不足且用户明确同意后，
  才局部读代码兜底。新项目已确认目标不冒充当前运行事实，存量项目已有知识作为更新基线。
- [x] Build 已定义问答协议：明确建设范围、复用已给来源、说明当前理解、按会改变结论的缺口提问，
  分清事实与未决项，不按模板机械追问，也不在四类知识间重复同一轮访谈。
- [x] 支持分批建设，只建设当前需要的知识，不要求首次执行时完成全部四类。

### 2.2 知识结构

四类既有内容设计共用知识系统内的 [rules-know](<./subsystems/2. 知识系统/rules-know/SKILL.md>)：

- 业务画像：业务图景、业务运作和业务方案；
- 运行模型：项目边界、核心对象、关键链路、数据流和外部依赖；
- 功能：按业务模块维护功能目标、可观察行为、规则、关键设计、协作与验证边界；
- 数据源：一个核心源及实际依赖的外部表，原生 SQL、真实或生成的数据集和一个总索引。

- [x] 全局存放规则进入 Runtime：知识在目标项目 `.harness/know/`，必要任务产物在 `.harness/tasks/`；
  不移动代码、测试代码或正式迁移，已有正式文档先确认沿用或迁移。
- [x] 以知识 `README.md` 总入口关联各类内容；功能按业务模块设入口，知识规模增加时按主题拆分。
  只创建有内容的目录与文档，不机械复制全套空模板。
- [x] 建设与维护共用 `rules-know`，入口只提供读取指引与索引，规则和模板按需加载。该 Skill 是
  Build 与 Know 沉淀的只读依赖，不承担执行、不成为 Router 节点，也不由 Know 检索加载。
- [x] 为业务画像、运行模型、模块、功能和总入口提供可裁剪轻模板；事实、目标和待确认项分别表达，
  不建立知识版本。
- [x] 数据源仅用一个 `数据源/index.json`，提供 JSON Schema 与示例；开头说明各源与核心源，随后
  登记所需库表、DDL 路径、数据集来源、覆盖与用法；不设源级第二份索引，不用 Markdown 代替 SQL。

本节已完成独立静态合同复核、JSON 示例字段与内部引用检查、文档链接检查；`rules-know` 与调用方通过
Skill 静态校验，并检查了功能沉淀与纯检索的依赖读取路径。临时项目已验证新接入、旧资源断链迁移为
规则 Skill，以及解绑保留知识和任务文件。实际 Build 问答及 Know 知识写入仍待第 8 节验证。

完成标准：新项目与存量项目都能通过可解释的问答形成需要的知识；用户跳过 `build` 时，正常需求链
仍然可运行。

## 3. 需求进入、知识可用性与 HARNESS 调度

目标：主会话以 HARNESS 为全局视角，只加载当前任务真正可用且必要的能力。

- [x] 明确宿主主会话是实际执行者；HARNESS 负责全局调度，Skill 向主会话提供规则和能力，不把每个
  Skill 当成独立角色。
- [x] 需求清楚时直接进入路线；只有不同理解会改变行动时才调用 `intent`。
- [x] Runtime 通过 `.harness/know/README.md` 是否存在判断知识入口，不维护绑定时的可用性快照；
  存在但不可读时返回真实问题，不等同于无知识。
- [x] Runtime 与 Know 合同已同步：无知识入口时直接跳过任务前检索，不先加载 Skill 再寻找入口；
  主会话与执行能力按需阅读当前代码、规则和用户材料。实际宿主行为待第 8 节验证。
- [x] 只有项目有知识入口，且缺少的共享事实会改变路线时，才调用 `know`；发现事实冲突且会影响本次
  行动时交还主会话判断或询问用户，不建设复杂冲突状态机。
- [x] 保持一次请求的必要目标、约束、当前产物和证据引用即可，不建设通用 Task/Action/状态对象。
- [x] HARNESS 汇总实际结果、知识沉淀结果、验证范围、Audit 结论和剩余问题后，本次响应自然结束。
  用户决定是否接受；系统不维护“人工已验收”或“已结束”状态。用户后续输入决定继续、修改、Debug
  还是新任务。
- [x] 用户审阅后可主动调用个人反馈能力 `cr`；反馈需要继续工作时，由主会话依据当前请求和事实处理。

本节公共合同已进入 Runtime、系统设计和项目规则；实际宿主是否按需加载、正确回流仍待第 8 节真实
任务验证。

完成标准：有知识和无知识的项目都能从同一个 HARNESS 入口进入需求，且无知识时不会产生无效 Skill
加载与上下文消耗。

## 4. Router、任务执行与 Coding

目标：Router 复用相近场景并开放组合当前可用 Skills，形成与任务实际复杂度和存量影响匹配的路线。

### 4.1 Router 场景化

- [x] HARNESS 根据项目绑定清单和实际 Skill 入口提供可用能力范围；Router 只选择当前已接入且可用的
  业务能力，能力缺失时返回缺口，不规划不存在的 Skill 或自动改变绑定。
- [x] Router 分别判断实现复杂度与存量影响：不按文件数、代码行数或任务标签分类；修改旧文件与改变
  旧行为分开判断，并关注必要性、影响更小方案和必须保持的功能。
- [x] Router 维护能力选择条件和必要依赖，明确选择具体 Skills、直接动作、顺序及模式；能力可按需
  省略、组合或重复使用，TDD、SDD 等简称不能替代实际 Skill 链路。
- [x] 增加少量覆盖局部修改、API 变更、已知与未知根因、TDD、共享行为、跨模块设计和单点能力请求的
  组合示例。示例只用于复用相近条件，不是路线全集或固定套餐；没有相近示例时按选择条件组合。
- [x] 简单、局部且影响明确的修改允许只执行 `coding → 修改`，不默认增加设计、测试、校验、审计或
  独立文档；已有用户与项目要求仍优先，跳过的检查不得描述为通过。
- [x] 将是否测试、测试模式和测试类型分开判断；TDD 使用具体测试 Skill 表达 Red、修改、Green 与
  回归，存量行为保护则要求相关测试在修改前后都通过。
- [x] Router 独立判断 spec、测试设计文档、测试结果和审计报告是否落盘，不因调用 Skill 自动生成；
  所有需保存的过程产物统一进入 `.harness/tasks/YYYYMMDD-任务简名/`，命名和沿用规则归 Runtime。
- [x] 约定后续只从真实使用中修正选择条件或增补高复用场景；当前阶段不枚举所有排列、入口和技术栈。

### 4.2 Coding 形态与路线继续

- [x] 保留 `coding` 作为修改生产代码时加载的个性化规则与本机环境约束；基础编码是 Agent 自身动作，
  不再新建 `dev` 或 `fix` 包装能力，也不在路线中反复解释执行主体。
- [x] 技术设计、Debug 结论和有效 Red 返回后继续既定路线；已选测试发现不改变目标与影响面的局部
  实现问题时，在当前路线修正并重跑，只有路线依据变化才重新规划。
- [x] 当前 Runtime、Router、README、项目规则、系统设计、TODO 和实际 Skill 拓扑使用一致的能力名称。

### 4.3 分支与危险操作保护

- [x] 软件工程领域 Manifest 声明 `main`、`master`、`test`、`pre` 为禁止直接提交的受保护分支，
  不把这组领域规则写死为所有 Harness 实例的框架规则。
- [x] `init` 将受管 `pre-commit` 与 `prepare-commit-msg` 安装到普通 Git 工作树的 `.git/hooks/`；
  后者不受 `--no-verify` 抑制。已有同名 Hook 记录类型、摘要或链接目标，原样备份并在允许提交的分支
  继续执行；符号链接 `.git`、linked worktree 和 `core.hooksPath` 暂不自动接管，避免影响其他仓库或
  共享 Hook。
- [x] Hook 在提交时停止并提示用户创建或切换开发分支，不自动切换；被拦截后未提交修改仍留在工作树，
  切到新分支可继续。门禁也不替代 Commit/Push 授权。
- [x] `relink` 修复或补齐受管 Hook，`doctor` 检查策略、内容、执行位和原 Hook 备份，`remove` 恢复
  接入前状态；正常异常路径纳入原绑定回滚。

完成标准：Router 能从当前可用 Skills 组成开放而具体的执行链路，简单任务不会被保障环节绑架，复杂
或影响存量的任务又能按需加入设计、测试和审计；Coding 不重复教授 Agent 已有能力，受保护分支有
明确的计算机侧拦截点。

## 5. Subagent、新会话与 Audit 编排

目标：只在隔离真正有价值时创建额外上下文，并让审计问题可靠回到主会话处理。

### 5.1 上下文编排

- [x] 定义主会话直接执行、创建 Subagent、创建新会话的适用边界。
- [x] Subagent 用于可封闭分支、并行工作和独立判断；新会话只用于需要用户长期单独跟进或当前上下文
  不应继续承载的任务，不把所有 Skill 变成 Agent。
- [x] 设计 Subagent 的任务命名、最小 Prompt、输入范围、结果格式、上下文回收和失败交回规则。

### 5.2 Audit 重构

- [x] 将 `audit` 设计为“主会话如何创建并管理审计 Subagent”的编排 Skill：它负责审计 Subagent 的
  创建 Prompt、只读/干净上下文要求、审计输入、输出以及后续复审协议。
- [x] Audit Subagent 只审计并返回问题、证据和建议，不直接修改；主会话判断意见并实施确认需要的修改。
- [x] 目标是首次审计没有问题；需要修正时，尽量一次修改、一次复审通过。
- [x] 限制主会话与 Audit Subagent 的意见协商，避免无限 Chat：一次提交当前候选或补充事实并返回结论
  计为一轮，初审是第一轮，最多三轮；
  主会话不同意时必须提供事实和依据，Audit 重新判断。达到上限后停止 Agent 间争论，能够确认的修改
  由主会话执行，仍有实质分歧则如实交给用户判断。
- [x] 审计再次运行时读取当前需求、当前差异和当前证据即可，不建设通用候选版本对象。

完成标准：Audit 的创建、一次反馈修正、重新审计和分歧停止条件都由同一 Skill 说明，主会话始终是
修改执行者，审计上下文不会反向接管任务。

## 6. 需求完成后的项目知识沉淀

目标：把当前需求形成的长期知识独立维护到项目知识中，不要求项目事先运行过 `build`。

- [x] Know 只提取后续理解、决策或执行所需的持续知识，没有持久变化时返回无需沉淀，不转录任务过程。
- [x] 项目已有知识入口时，沿业务画像、运行模型、模块化功能或数据源的现有内容增量更新。
- [x] 项目此前没有知识库时，允许 `know` 为本次确有价值的结果建立最小知识入口和必要目录；不得因为
  没执行完整 `build` 而阻断首次沉淀。
- [x] 四类建设参考由 `rules-know` 统一维护，Build 与 Know 共用正文结构、导航、模板与维护质量；建设和
  增量维护仍保留不同入口与取材流程，不合成一个大 Skill。
- [x] 知识只维护当前有效内容、事实来源和仍未确认事项，不引入知识版本、敏感信息分类、脱敏流程或
  面向人工验收的状态模型。
- [ ] 讨论哪些知识变化需要独立审计，避免每次微小沉淀都机械增加 Audit；写入后回读当前知识即可。

完成标准：无知识库项目的首个需求也能在不运行完整 `build` 的情况下形成最小、可导航的长期知识；
过程文档不会被直接当作知识正文。

## 7. Retro 证据入口与 Harness 演进

目标：Retro 只基于可获得的计算机侧事实提出改进候选，用户决定是否启动后续维护。

- [ ] 比较 Retro 的证据来源：
  - 当前会话及其最终结果；
  - HARNESS 在任务过程中保留的最小事件摘要；
  - Hook 记录的能力选择、工具结果、测试、Audit 和反馈事件。
- [ ] 不默认全量读取所有历史会话，也不保存隐藏思维过程；先确定 Retro 判断真实问题所需的最小事实。
- [ ] 定义哪些事件值得记录、记录在哪里、如何关联当前项目和需求，以及什么时候可以删除；当前不建设
  通用事件平台或复杂状态机。
- [ ] 定义 Retro 的触发方式：用户显式复盘、出现有证据的重复问题，或主会话建议复盘后由用户决定；
  不把每次需求完成都强制变成演进阶段。
- [ ] Retro 只输出问题证据和改进候选。所谓“批准实施”仅指用户决定另开一次 Harness 维护任务；随后
  由主会话按候选修改正确载体，并按需要加载 `skill-creator`、`skill-neat` 或其他维护能力。
- [ ] Release、发布和回退由用户另行处理，不进入当前系统实现，也不由 Retro 自动执行或限制。
- [ ] 建立最小演进记录：问题证据、用户选择、实际修改和验证结果；不自动把一次案例升级为框架规则。

完成标准：Retro 能解释证据来自哪里、为什么形成候选以及候选交给谁；没有证据时不会自动改 Harness，
用户不启动维护任务时系统事实不发生变化。

## 8. 二次实现后的真实验证

本阶段在前述设计和实现完成后开始，不作为当前设计讨论的阻断项。

- [ ] 在真实业务项目验证 `init/relink/doctor/remove/list`，包括 Runtime 加载、Skill 发现、Git Hook
  提交拦截与源目录移动后的恢复；当前仅完成临时 Git 目录受控冒烟，不据此启用主链。
- [ ] 验证无知识库项目能够跳过任务前 `know` 并完成一次简单需求。
- [ ] 分别验证新项目和存量项目的可选问答式 `build`。
- [ ] 验证一条边界清楚的小修改路线和一条复杂需求路线。
- [ ] 验证 TDD 或 Debug 结果回到主会话后完成代码修改和最终测试。
- [ ] 验证 Audit 一次通过，以及一次反馈修改后复审通过；同时验证三轮上限不会无限对话。
- [ ] 验证用户反馈重新进入开发路线，以及 Agent 外部操作的授权保护。
- [ ] 验证无完整知识库时的首次需求后知识沉淀。
- [ ] 验证 Retro 从最小证据形成候选，并由独立 Harness 维护任务消费。
- [ ] 只在这些代表性链路通过后更新 README 和 Runtime 的“未启用”状态；后续缺失能力由真实使用和
  演进机制逐步补充。

完成标准：当前软件工程 Harness 的主链和代表性回流能够在真实业务项目中完成；未覆盖的新技术入口
继续作为演进候选，不阻止当前系统迭代启用。
