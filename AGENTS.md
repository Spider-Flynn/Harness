# Harness Engineering 项目规则

## 项目边界

本目录维护 Harness Engineering 的架构、运行规则、流程、Skills、专用 Agent 和项目接入工具。
业务项目的需求事实、代码、项目知识和本地规则仍由业务项目维护，不写入本目录。

## 事实载体

- `docs/`：系统设计与实施规划。
- `runtime/HARNESS.md`：未来接入业务项目时的全局运行入口。
- `flows/`：系统级整体流程及其 Skill 编排，不承载单个 Skill 的内部实现规则。
- `skills/`：Harness 独立能力源码；一级分类为 `1. 意图接入系统` 至 `7. 演进系统`、
  `8. 横向能力` 和 `9. 额外能力`，第二层放置 Skill。
- `agents/`：专用 Agent 配置。
- `scripts/`：项目接入与维护工具。

## 维护规则

- `Harness/skills/` 中的既有开发 Skills 由 Powers 能力复制形成独立基线，新能力放入对应子系统目录；本目录不是镜像或安装入口。
- Powers 与 Harness 的同名 Skill 不自动同步；需要吸收对方变化时，先比较差异、确认适用边界，再单向合入。
- 修改 Harness Skill 只修改本目录副本，不回写 Powers 根目录的 `../skills/`，除非用户另行明确要求。
- 实现单个子系统时，只实现该子系统的职责、载体和能力规则。上游与下游只用于确认职责边界；
  跨系统时序、状态、回流、恢复、终止和交接合同统一归整体编排，不写入子系统能力。
- `router` Skill 根据任务风险选择执行路线，只决定是否需要技术设计、稳定文档、测试设计和
  TDD 测试先行，不选择或调用执行系统 Skill，也不定义执行方法、文档结构、测试实现和代码
  产物；功能型 Skill 只负责自身怎么做，不维护流程选择规则。
- 功能型 Skill 发现输入缺失、职责越界或流程不适用时，返回问题和仍有效事实，由运行时交给对应上游或 `router`，不自行选择下一 Skill。
- 子系统能力按架构顺序逐个实现；现有架构与子系统设计是实施输入。除状态和直接引用同步外，
  不因当前子系统的实现判断修改其他子系统或整体编排；发现设计问题时另行记录并与用户对齐。
- `skills/1. 意图接入系统/cooper/` 与 `skills/9. 额外能力/mock/` 是仅供本机运行的能力
  副本，由 `.gitignore` 排除，不作为可提交共享源码。
- 尚未完成设计和验证的运行入口、流程、Agent 或脚本必须标明未启用，不得写成已落地能力。
- Harness 接入业务项目后，运行规则与业务项目规则分开维护；冲突由业务项目中的明确规则和人工裁决处理。
- `skills/7. 演进系统/` 仅维护 Harness 自身：`retro` 产出改进候选，人工批准后由 `skill-creator`、`skill-neat` 或其他实现能力执行；不进入业务项目的 `intent`、`know` 或 `router`。
- `skills/8. 横向能力/subagent/` 是 Harness 隔离维护的编排能力，可被多个子系统加载，但不构成独立时序节点。

## 当前工作状态

- 系统架构、七个子系统、最小 Skill 拓扑和整体运行闭环已完成初步设计；第 7 系统为 Harness 演进控制面，复盘只是其分析动作。目标目录、核心 Skills、
  运行记录模板和本地接入工具已完成初步实现，尚未经过真实任务验证。
- 已从 Harness 能力中移除 `use-power`；`router` 只根据风险形成执行路线，不选择具体 Skill。
- `intent` 输出已确认任务意图，`know` 输出由可追溯信息元组成的任务信息集合，`router` 只选择
  完整执行流程；整体主链试运行前均保持未启用。
- 已复制 `cooper` 和 `mock` 作为本机能力，并由 `.gitignore` 排除。
- 13 个业务项目固定投影 Skill、三个条件扩展能力和两个演进专用 Skill 已初步形成；`scripts/harness.py` 已实现
  `init/doctor/update/remove`，但只在临时目录试用，Harness 整体仍未启用。
- 下一步选择经授权的低风险业务项目运行正常链和问题回流；实施发现会改变
  架构的设计问题时先记录并与用户对齐，不在实现任务中静默改设计。
