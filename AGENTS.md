# Harness Engineering 项目规则

## 项目边界

本目录维护 Harness 框架、当前软件工程领域实例的系统设计与实现，以及项目维护与接入工具。
Harness 框架围绕 Agent 和 LLM 定义跨领域可复用的运行、编排、信息所有权和扩展边界；软件工程
领域实例维护当前五个业务任务职责域、整体开发编排和能力实现，Harness 框架另设演进控制面。业务项目的需求事实、代码、项目知识和
本地规则仍由业务项目维护，不写入本目录。

## 事实载体

- `README.md`：面向使用者的项目介绍、接入方式和当前可用范围，不承载维护者工作规则。
- `TODO.md`：当前整体重构、二次实现、验证和未决问题。
- `docs/系统设计.md`：已确认的框架与当前软件工程领域实例设计事实。
- `docs/接入设计.md`：接入工具的模块职责、命令对应关系和存储边界。
- `docs/演进记录.md`：Harness 自身复盘改进候选的采纳、修改载体与验证历史事实。
- `docs/外部实践与设计候选.md`：外部资料提炼和待讨论设计候选，不属于已确认设计或实施计划。
- `cmd/`：本仓库与 AI 交互的分命令指引，通过统一 CLI 执行确定性操作。
- `runtime/HARNESS.md`：接入业务项目时的唯一运行合同。
- `systems/`：领域系统 Manifest，声明 Runtime、各类能力和资源投影，不存放子系统实现或规范正文。
- `subsystems/2. 知识系统/rules-know/`：供 `build` 和 `know` 沉淀使用的只读规则 Skill，
  维护共享规范、类型规则与可裁剪模板，不在调用方复制维护。
- `subsystems/`：按职责域组织子系统实现；一级分类为 `1. 意图接入系统` 至 `6. 演进系统`、
  `7. 横向能力` 和 `8. 额外能力`，各目录可同时维护 Skills 与其共享规范、模板等资源。
- `scripts/`：仓库维护、能力投影和业务项目接入工具。
- `data/`：本仓库本地数据，保存绑定项目索引、锁和必要备份；按需生成并整体 Git 忽略，不写入用户
  主目录。目标项目的绑定清单仍是绑定事实，不在此复制业务项目正式资产。

事实发生冲突时，先以当前仓库文件、实际目录和可重复验证结果为准，再修正文档、脚本或设计中的
过期描述。目标设计不得被写成已经落地，当前实现也不得反向覆盖尚未确认的目标设计。
当前进度与未验证事项以 `TODO.md` 为准，本文件只维护本仓库长期适用的规则，不重复维护进度副本。

## 项目接入对话入口

用户在本仓库会话中请求 `init`、`relink`、`doctor`、`remove`、`list`，或明确要求对应的项目绑定、
修复、检查、解绑、列出操作时，仅加载 `cmd/<命令>.md`，据此交互并调用接入脚本。
仅讨论或引用命令、业务代码初始化及 `git init` 不触发此入口；普通维护不加载命令提示词。

## 框架、领域实例与实现边界

- **Harness 框架层**：定义任何领域实例都需要遵守的全局编排职责、信息和产物所有权、领域扩展
  接口、确定性治理边界及框架演进规则，不定义固定子系统数量、软件开发流程或领域产物格式。
- **领域实例层**：定义特定目标领域的子系统、职责、整体编排、过程产物、结果产物和领域门禁。
  当前领域实例是软件工程 Harness；未来其他领域可以采用不同子系统与流程。
- **实现层**：用 Skills、脚本、模板、连接器、Agent 配置和宿主适配实现已确认的框架或领域合同，
  不用实现现状反向定义上层设计。

Harness 是全局编排者：决定是否进入意图或知识能力，调用 `router` 形成当前需求的执行路线，让宿主
主会话按路线加载能力，处理反馈与重新规划，并汇总当前计算机侧结果。宿主主会话是默认执行者；
Skill 是能力与规则单元，不代表独立角色。`router` 负责选择路线中的具体能力、顺序、稳定产物要求、
测试策略和审计门禁，但不直接执行能力。业务项目保存代码、设计、测试、项目知识等正式资产。
Harness 不复制维护平行正式资产，子系统也不自行管理跨系统生命周期。

框架统一将 Harness 管理的知识与需持久化的任务产物放在目标项目 `.harness/` 下；软件工程实例用
`know/` 保存知识、`tasks/` 保存必要的任务产物。具体类型、正文结构和质量属于领域规范。代码、测试
代码及正式迁移保持原位；已有正式文档先确认沿用或迁移，不建立平行副本。接入工具的 Runtime、资源
链接与绑定记录不拥有这些项目资产，解绑和重建绑定必须保留它们。

处理规则、问题、反馈或修改前先确认其归属：

1. 不依赖具体领域语义，且会约束所有领域实例的运行、编排、信息所有权、扩展或演进方式，归框架层。
2. 只改变软件工程等特定领域的子系统职责、路线、产物、结果或验收，归对应领域实例层。
3. 只改变某个 Skill、脚本、模板、连接器或宿主适配如何履行既有合同，归实现层。
4. 同一问题跨越多层时，先确认上层合同，再分别处理受影响实现；不得用底层补丁掩盖上层缺口。
5. 无法判断归属，或仅有单个案例不足以支持通用规则时，先进入设计候选并与用户讨论，不直接写入
   稳定设计、项目规则、TODO 或具体能力。

框架层变化必须检查对所有现有领域实例和接入合同的影响；领域实例变化不得自动上升为框架规则；
实现层变化不得静默改变框架或领域合同。一次软件工程任务中的失败只有在问题确实独立于软件工程
语义时，才可以成为框架改进候选。

## Git 与公开仓库

- 本项目作为独立公开仓库维护，默认分支为 `main`；提交内容必须适合公开，不得包含凭据、私有业务
  数据、内部地址、个人临时文件或无法公开授权的材料。
- `subsystems/1. 意图接入系统/cooper/` 与 `subsystems/8. 额外能力/mock/` 是永久仅限本机的能力目录。
  两个目录及其未来新增内容始终由根目录 `.gitignore` 排除，不得使用强制添加绕过，也不得在公开
  文档中假设其源码随仓库提供。
- 提交前至少核对 `git status`、待提交差异和忽略规则命中情况；只提交本次确认范围内的变化，不把
  缓存、生成物或无关工作区内容带入提交。
- README、架构、运行规则、脚本和 Skill 拓扑发生直接合同变化时，同一变更中同步所有受影响的事实
  载体；无法同步时明确记录不一致、影响和后续收敛条件。
  职责撤销或调整时，一并清理旧调用、交接、后续动作及仅用于解释旧设计的语句。
- 创建提交、推送、发布或修改 GitHub 仓库设置均以用户对该外部操作的明确授权为准。

## 维护规则

- `scripts/harness.py` 只保留 CLI 入口；参数和分派归 `scripts/harness_cli/cli.py`，各命令流程归
  `scripts/harness_cli/commands/`，公共逻辑按职责维护，不回收为单文件或混杂的公共工具文件。
  `cmd/` 只维护 AI 交互与调用指引，不直接实现绑定操作；命令合同改变时同步对应指引和使用说明。
- 软件工程实例接入普通 Git 工作树时，按领域 Manifest 将受管 `pre-commit` 和 `prepare-commit-msg`
  安装到目标项目 `.git/hooks/`，阻止在 `main`、`master`、`test`、`pre` 直接提交；后者保证
  `--no-verify` 不能跳过该分支门禁。已有同名 Hook 必须分别记录类型、摘要或链接目标，原样备份并在
  非受保护分支继续执行；`relink` 负责同步，`doctor` 负责检查，`remove` 负责恢复接入前状态。Hook
  只拦截提交，不阻止编辑，也不替 Agent 切换分支或授予 Commit 权限。符号链接 `.git`、linked
  worktree 或已配置 `core.hooksPath` 时不得自动接管可能属于其他工作树或共享配置的 Hook 目录。
- `subsystems/` 中的既有能力由 Powers 能力复制形成独立基线，新能力及其共享资源放入对应职责域目录；
  本目录不是镜像或安装入口。子系统不等同于 Skill，资源不因与 Skill 同目录就被投影为能力。
- Powers 与 Harness 的同名 Skill 不自动同步；需要吸收对方变化时，先比较差异、确认适用边界，再单向合入。
- 修改 Harness Skill 只修改本目录副本，不回写 Powers 根目录的 `../skills/`，除非用户另行明确要求。
- 实现单个子系统时，只实现该子系统的职责、载体和能力规则。上游与下游只用于确认职责边界；
  跨系统时序、状态、回流、恢复、终止和交接合同统一归整体编排，不写入子系统能力。
- `router` Skill 维护软件工程实例的能力选择条件、必要依赖和代表性场景；先复用相近场景，再按目标、
  实现复杂度与存量影响组合当前可用 Skills、顺序、测试模式和审计，独立决定文档要求。场景不构成
  封闭路线全集，模式名不替代具体 Skill 链路。简单局部改动可跳过测试、校验和审计；已有明确要求
  仍须遵守。它不执行能力，也不定义专业内容与内部方法，产物目录和命名统一归 Runtime。
- 功能型 Skill 发现输入缺失、职责越界或流程不适用时，返回问题和仍有效事实，由 HARNESS 交给对应
  上游或 `router`，不自行选择下一 Skill。
- `build` 是用户按需启动的项目知识建设能力，不是 Harness 启用门禁。没有项目知识库时，HARNESS
  跳过任务前 `know` 检索；主会话仍可完成需求，任务后有长期价值的结果仍允许建立最小知识入口。
  新项目与存量项目均先索要或确认用户信息源，通过问答建设；读代码是其他来源不足且用户明确同意后
  的局部兜底，不是 Build 的默认起点。
- `know` 的项目知识沉淀从任务过程产物、稳定结果和可回查事实中提取具有持续价值的内容，按业务画像、
  运行模型、功能和数据源维护。过程产物是知识来源，不替代知识内容；是否进入沉淀由 HARNESS 判断，
  具体提取、组织、写入和验证由 `know` 负责。不为项目知识建立版本或敏感信息治理子系统。
- 知识建设和增量维护共用 `rules-know` 的结构、质量与维护规则。该 Skill 随系统投影到
  `.agents/skills/rules-know/`，只作为 Build 与 Know 沉淀的只读依赖，不由 Router 安排独立节点，
  不承接问答、取材或写入；Know 检索不加载它。Build 与 Know 保留各自执行职责，不因读取规则依赖
  获得跨系统调度权。人读文档使用可裁剪模板，数据源只维护一个 `index.json`。
- 生产代码由宿主主会话实现并加载 `coding`；`coding` 只提供个性化编码习惯和本机环境使用约束，
  不负责路线判断、测试选择、代码级自检或最终验证。路线只需说明具体能力与动作，不反复解释执行
  主体，不为基础编码另建 `dev` 或 `fix` 包装能力。
- 子系统能力按 `TODO.md` 确认的整体设计和运行合同逐步实现。除状态和直接引用同步外，不因单个
  Skill 的实现判断修改其他职责域或整体编排；发现设计问题时先记录并与用户对齐。
- 尚未完成设计和验证的运行入口、流程、Agent 或脚本必须标明未启用，不得写成已落地能力。
- 验证报告只声明实际执行的检查及其覆盖范围，不把静态检查、Mock 或脚本存在描述成真实业务链路通过。
- Harness 接入业务项目后，运行规则与业务项目规则分开维护；冲突由业务项目中的明确规则和人工裁决处理。
- 人工 Review、风险接受、发布和回退由用户自行决定。Harness 只管理 Agent 的计算机侧工作；
  执行 Commit、Push、PR、部署、发布等外部写入前必须获得具体授权，并
  优先使用分支检查、Hook 或宿主审批等确定性保护。`subsystems/8. 额外能力/cr/` 只在用户审阅后主动提出
  疑问、风险、补充或反驳时使用，不由 HARNESS 调度，也不作为业务项目默认投影能力。
- 独立审计由主会话按 `audit` Skill 创建 Audit Subagent；Audit 只返回问题、证据和建议，主会话负责
  判断与修改。审计协商必须有限轮次，具体 Prompt、复审和分歧停止规则归 `audit` Skill，不扩张为
  全局任务状态机。
- `subsystems/6. 演进系统/` 仅维护 Harness 自身：`retro` 从可获得的计算机侧证据产出改进候选，用户决定
  是否另行启动 Harness 维护任务，再由主会话按需加载 `skill-creator`、`skill-neat` 或其他维护能力；
  它不进入业务项目的 `intent`、`know` 或 `router`，也不自动发布或回退。
- `subsystems/7. 横向能力/subagent/` 是 Harness 隔离维护的编排能力，可被多个子系统加载，但不构成独立时序节点。
