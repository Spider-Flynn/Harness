# doctor：检查绑定

确定目标项目；缺少目标时根据本仓库 `data/projects.json` 帮助选择，索引不可用则询问路径。
从本仓库调用 `python3 scripts/harness.py doctor --project "<项目绝对路径>"`。

区分绑定错误与辅助索引提示，解释影响和建议操作。用户仅要求检查时，不顺带执行修复。
检查包括 Git Hook 策略、内容、执行位和原 Hook 备份。通过只表示绑定符合脚本检查项，不代表宿主
加载规则或业务主链已经验证。
